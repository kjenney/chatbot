# Required dependencies
ollama>=0.6.0
flask>=3.0.0
requests>=2.31.0
ddgs>=9.14.0
tabulate>=0.9.0

# Gmail agent
google-api-python-client>=2.100.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.1.0

# Note: SQLite3 is included in Python's standard library
# GitRepoAgent requires git to be installed and available in PATH

# Testing
pytest>=8.0.0
pytest-mock>=3.12.0

# Optional: For other AI providers (if you want to switch)
# openai>=1.0.0
# anthropic>=0.8.0
